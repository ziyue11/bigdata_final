# 大数据期末需求文档（按分工）

**项目目录结构** \+ **A/B/C 三个角色操作手册**。整体设计依据是课程评分要求：爬虫采集 5–7 分、数据分析 1–3 分、可视化 4–6 分、整体分析 2–4 分，同时额外争取 Agent/Skills 最高 5 分加分；课程文件也明确要求提交研究实践报告、PPT、源代码、数据文件，并鼓励提交 AI 工具使用说明、Skills 目录、运行日志、截图或输入输出样例。

---

# 项目目录结构

项目名建议：

```Plain Text
financial_risk_warning/
```

完整目录：

```Plain Text
financial_risk_warning/
├── README.md
├── requirements.txt
├── config.yaml
│
├── data/
│   ├── raw/                         # A 输出：原始爬虫数据
│   │   ├── regulation_nfra_raw.csv
│   │   ├── regulation_csrc_raw.csv
│   │   ├── news_raw.csv
│   │   └── comment_raw.csv
│   │
│   ├── processed/                   # A/B 输出：清洗与统一后的数据
│   │   ├── regulation_cleaned.csv
│   │   ├── news_cleaned.csv
│   │   ├── comment_cleaned.csv
│   │   ├── llm_extracted.csv
│   │   └── risk_event_table.csv
│   │
│   └── analysis/                    # B 输出：供 C 可视化使用的分析结果表
│       ├── summary_stats.csv
│       ├── risk_type_stats.csv
│       ├── risk_time_trend.csv
│       ├── region_risk_stats.csv
│       ├── entity_risk_rank.csv
│       ├── sentiment_trend.csv
│       ├── risk_relation_matrix.csv
│       └── risk_warning_cases.csv
│
├── src/
│   ├── crawler/                     # A 负责
│   │   ├── crawl_nfra.py
│   │   ├── crawl_csrc.py
│   │   ├── crawl_eastmoney_news.py
│   │   ├── crawl_eastmoney_guba.py
│   │   └── run_crawlers.py
│   │
│   ├── preprocess/                  # A 主责，B 参与字段确认
│   │   ├── clean_regulation.py
│   │   ├── clean_news.py
│   │   ├── clean_comment.py
│   │   └── run_preprocess.py
│   │
│   ├── llm_extract/                 # B 负责
│   │   ├── prompt_templates.py
│   │   ├── extract_risk_fields.py
│   │   ├── build_risk_event_table.py
│   │   └── manual_review_sample.py
│   │
│   ├── analysis/                    # B 负责
│   │   └── run_analysis.py
│   │
│   ├── visualization/               # C 负责
│   │   ├── app.py
│   │   ├── charts.py
│   │   └── generate_report_summary.py
│   │
│   └── run_pipeline.py              # 共同维护，C 负责最终串联
│
├── skills/
│   ├── multi_source_crawler_skill/          # A 负责
│   │   ├── SKILL.md
│   │   ├── run_example.md
│   │   ├── sample_input.yaml
│   │   └── sample_output.csv
│   │
│   ├── field_cleaning_mapping_skill/        # A 负责，B 协助字段口径
│   │   ├── SKILL.md
│   │   ├── run_example.md
│   │   ├── sample_input.csv
│   │   └── sample_output.csv
│   │
│   ├── llm_risk_extract_analysis_skill/     # B 负责
│   │   ├── SKILL.md
│   │   ├── prompt_template.md
│   │   ├── run_example.md
│   │   └── sample_output.csv
│   │
│   └── risk_visual_report_skill/            # C 负责
│       ├── SKILL.md
│       ├── run_example.md
│       ├── sample_dashboard.png
│       └── sample_report_summary.md
│
├── outputs/
│   ├── figures/                     # C 输出：图表图片
│   ├── logs/                        # A/B/C 各自放运行日志
│   └── screenshots/                 # AI 使用截图、运行截图、看板截图
│
└── docs/
    ├── ai_usage_records/            # AI 对话、提示词、Codex 调试记录
    ├── manual_review/               # B 输出：人工审核表
    ├── report/                      # 共同输出：研究实践报告材料
    └── ppt/                         # 共同输出：课堂汇报 PPT 材料
```

统一运行顺序：

```Plain Text
python src/crawler/run_crawlers.py
python src/preprocess/run_preprocess.py
python src/llm_extract/extract_risk_fields.py
python src/llm_extract/build_risk_event_table.py
python src/analysis/run_analysis.py
streamlit run src/visualization/app.py
```

最终总控命令：

```Plain Text
python src/run_pipeline.py
```

统一文件规范：

# A 角色操作手册：多源数据采集 \+ 单源清洗标准化

### A 的角色定位

A 是**数据入口负责人**。核心任务是保证项目有高质量、可用、可追溯的数据来源。A 不只是写爬虫，还要负责每个网站的字段设计、数据质量检查、原始数据输出、单源初步清洗，并把“多源数据采集”和“字段清洗映射”封装成 Skill。

A 的工作直接影响爬虫采集得分。课程爬虫采集评分重点包括：数据源质量、数据完整性、代码规范性、工具使用情况，并要求报告中说明源网站链接、数据采集过程、数据源选择理由、爬取策略、清洗预处理步骤。

### A 负责的数据源

最低目标：总有效数据不少于 1000 条。理想目标：2500 条以上。

如果某个网站爬取难度过高，可以保留 2–3 个稳定数据源，但必须保证数据量和字段质量。

### A 需要输出的原始字段

#### 监管处罚原始表

`data/raw/regulation_nfra_raw.csv`

```Plain Text
source,title,publish_time,decision_no,punished_entity,violation_text,penalty_text,penalty_amount_raw,agency,url,crawl_time
```

`data/raw/regulation_csrc_raw.csv`

```Plain Text
source,title,publish_time,party,violation_text,penalty_text,penalty_amount_raw,market_ban_raw,url,crawl_time
```

字段说明：

#### 新闻原始表

`data/raw/news_raw.csv`

```Plain Text
source,title,publish_time,channel,content,url,crawl_time
```

#### 评论/股吧原始表

`data/raw/comment_raw.csv`

```Plain Text
source,title,publish_time,stock_or_entity,content,read_count,comment_count,url,crawl_time
```

### A 负责的清洗输出

A 负责单源清洗，输出给 B 使用。

`data/processed/regulation_cleaned.csv`

```Plain Text
record_id,source_type,source,title,event_date,entity_name,raw_text,penalty_amount,agency,region,url
```

`data/processed/news_cleaned.csv`

```Plain Text
record_id,source_type,source,title,event_date,content,mentioned_entity,channel,url
```

`data/processed/comment_cleaned.csv`

```Plain Text
record_id,source_type,source,title,event_date,content,mentioned_entity,read_count,comment_count,url
```

清洗规则：

### A 的代码文件

```Plain Text
src/crawler/crawl_nfra.py
src/crawler/crawl_csrc.py
src/crawler/crawl_eastmoney_news.py
src/crawler/crawl_eastmoney_guba.py
src/crawler/run_crawlers.py

src/preprocess/clean_regulation.py
src/preprocess/clean_news.py
src/preprocess/clean_comment.py
src/preprocess/run_preprocess.py
```

每个爬虫建议统一函数接口：

```Plain Text
def crawl() -> list[dict]:
    """
    返回当前网站的原始数据列表。
    每条数据是一个 dict。
    """
```

`run_crawlers.py` 负责统一运行并保存 CSV。

### A 和 B 的交接

A 交给 B 的文件：

```Plain Text
data/processed/regulation_cleaned.csv
data/processed/news_cleaned.csv
data/processed/comment_cleaned.csv
```

A 同时需要提供：

```Plain Text
字段说明文档
数据量统计
缺失值情况说明
爬虫运行日志
至少 3 张截图：网站页面、Codex 辅助开发、运行结果
```

交接检查标准：

### A 负责的 Skill

#### `multi_source_crawler_skill`

功能：根据配置运行多源爬虫，输出 `data/raw/*.csv`。

必须包含：

```Plain Text
SKILL.md
run_example.md
sample_input.yaml
sample_output.csv
运行截图
```

#### `field_cleaning_mapping_skill`

功能：读取原始 CSV，完成去重、日期格式化、金额清洗、文本清洗，输出 `data/processed/*_cleaned.csv`。

必须包含：

```Plain Text
SKILL.md
run_example.md
sample_input.csv
sample_output.csv
清洗前后对比截图
```

### A 的最终完成标准

```Plain Text
1. 完成 2–4 个网站的数据采集；
2. 总有效数据不少于 1000 条，理想为 2500+；
3. 输出 raw 和 processed 两级 CSV；
4. 数据字段与接口文档一致；
5. 代码可运行，能通过 run_crawlers.py 和 run_preprocess.py 复现；
6. 有 Codex 使用截图、运行日志、字段说明；
7. 完成 multi_source_crawler_skill 和 field_cleaning_mapping_skill。
```

# B 角色操作手册：LLM 风险抽取 \+ 风险事件表 \+ 数据分析

### B 的角色定位

B 是**数据理解与分析负责人**。B 的核心任务是把 A 清洗后的文本数据进一步转化为可分析的风险字段，并完成单源分析和综合分析。B 是本项目体现“大模型能力”的核心角色。

B 不负责网页爬取，也不负责可视化页面开发；B 的重点是：**LLM 抽取字段准不准、风险事件表能不能支撑分析、分析结果表能不能交给 C 画图。**

课程数据分析高分要求包括多角度分析，例如描述性统计、时间序列分析、相关性分析、聚类分析等，且要求分析结果准确、能展示数据特征和趋势，并从数据中挖掘有价值的见解。

### B 的输入文件

B 从 A 接收：

```Plain Text
data/processed/regulation_cleaned.csv
data/processed/news_cleaned.csv
data/processed/comment_cleaned.csv
```

B 不直接修改 A 的原始数据。如果发现字段缺失或数据异常，需要反馈给 A，而不是私自改接口。

### B 需要设计的 LLM 抽取字段

统一风险字段如下：

风险类型必须固定，避免模型自由发挥：

```Plain Text
合规风险、信贷风险、信息披露风险、内部控制风险、市场操纵风险、消费者权益风险、经营风险、流动性风险、舆情风险、其他风险
```

### B 的 LLM 抽取对象

LLM 输出必须是结构化 JSON，方便程序解析。

示例输出格式：

```Plain Text
{
  "entity_name": "某某银行",
  "entity_type": "银行",
  "region": "浙江",
  "risk_type": "合规风险",
  "risk_level": "中",
  "sentiment": "负面",
  "sentiment_score": -0.72,
  "violation_reason": "贷款管理不审慎，相关业务存在违规操作",
  "impact_scope": "机构自身、客户权益",
  "summary": "该机构因贷款管理问题受到监管处罚",
  "llm_confidence": 0.86
}
```

### B 需要构建的核心表

B 最重要的输出是：

```Plain Text
data/processed/risk_event_table.csv
```

字段：

```Plain Text
event_id,source_type,source,event_date,entity_name,entity_type,region,risk_type,risk_level,risk_score,sentiment,sentiment_score,violation_reason,impact_scope,penalty_amount,heat_score,summary,url,llm_confidence,manual_check
```

说明：

### B 的风险评分规则

建议先用可解释规则，不做复杂模型。

```Plain Text
risk_score = 基础分 + 风险等级分 + 处罚金额分 + 负面情绪分 + 热度分 + 重复出现分
```

参考规则：

最后限制在 0–100。

### B 的分析输出文件

B 必须输出给 C 使用的结果表，字段不要随意改。

#### 1\. `summary_stats.csv`

```Plain Text
metric,value
```

包含：

```Plain Text
total_events
regulation_events
news_events
comment_events
high_risk_events
total_penalty_amount
negative_sentiment_ratio
```

#### 2\. `risk_type_stats.csv`

```Plain Text
risk_type,event_count,total_penalty_amount,avg_risk_score,high_risk_count
```

#### 3\. `risk_time_trend.csv`

```Plain Text
month,regulation_count,news_count,comment_count,high_risk_count,total_penalty_amount,avg_risk_score
```

#### 4\. `region_risk_stats.csv`

```Plain Text
region,event_count,total_penalty_amount,high_risk_count,avg_risk_score
```

#### 5\. `entity_risk_rank.csv`

```Plain Text
entity_name,entity_type,region,event_count,total_penalty_amount,avg_risk_score,risk_level
```

#### 6\. `sentiment_trend.csv`

```Plain Text
month,negative_news_count,negative_comment_count,negative_ratio,avg_sentiment_score
```

#### 7\. `risk_relation_matrix.csv`

```Plain Text
entity_type,risk_type,event_count,avg_risk_score
```

#### 8\. `risk_warning_cases.csv`

```Plain Text
entity_name,warning_reason,evidence_sources,risk_score,suggested_action
```

### B 的人工审核要求

B 必须做 LLM 抽取结果审核。

输出：

```Plain Text
docs/manual_review/manual_review.csv
```

字段：

```Plain Text
sample_id,text_snippet,llm_risk_type,manual_risk_type,is_risk_type_correct,llm_risk_level,manual_risk_level,is_level_correct,llm_sentiment,manual_sentiment,is_sentiment_correct,remark
```

最低审核 50 条，理想 100 条。报告里写：

```Plain Text
我们对 LLM 抽取结果进行了人工抽样审核，重点检查风险类型、风险等级、主体识别和情绪判断是否合理。对于错误样例，我们调整了提示词和风险类型字典。
```

### B 的代码文件

```Plain Text
src/llm_extract/prompt_templates.py
src/llm_extract/extract_risk_fields.py
src/llm_extract/build_risk_event_table.py
src/llm_extract/manual_review_sample.py
src/analysis/run_analysis.py
```

### B 和 C 的交接

B 交给 C：

```Plain Text
data/processed/risk_event_table.csv
data/analysis/*.csv
```

同时提供：

```Plain Text
每个 analysis CSV 的字段说明
每个指标的含义
风险评分规则说明
3–5 条典型风险案例
```

交接标准：

### B 负责的 Skill

#### `llm_risk_extract_analysis_skill`

功能：读取清洗后的 CSV，调用 LLM 抽取风险字段，生成 `risk_event_table.csv` 和 `analysis/*.csv`。

必须包含：

```Plain Text
SKILL.md
prompt_template.md
run_example.md
sample_output.csv
LLM 输入输出样例
人工审核说明
```

### B 的最终完成标准

```Plain Text
1. 完成 LLM 风险字段抽取；
2. 构建 risk_event_table.csv；
3. 完成至少 4 类以上分析，理想为 6–8 类；
4. 输出所有 analysis/*.csv；
5. 完成人工抽样审核；
6. 有 GPT 使用截图、提示词、输入输出样例；
7. 完成 llm_risk_extract_analysis_skill。
```

# C 角色定位：交互式风险预警看板负责人

C 的任务不是做整体分析，也不是封装 Skill，而是把 B 输出的分析结果做成一个**可运行的交互式网页看板**。这个看板需要让用户能够通过筛选条件查看不同时间、地区、风险类型、风险等级、主体类型下的金融风险分布情况。

C 的核心产出不是 PNG，而是：

```Plain Text
src/visualization/app.py        # 交互式网页主程序
src/visualization/charts.py     # 图表组件函数
src/visualization/utils.py      # 可选：数据读取、筛选、格式化函数
README 中的运行说明             # 如何启动看板
```

PNG 截图只是从网页运行结果中截取，用于报告和 PPT，不是主要成果。

---

## 1\. C 的输入文件

C 不读取原始爬虫数据，也不重新做复杂分析。C 只读取 B 已经处理好的两个层级数据：

```Plain Text
data/processed/risk_event_table.csv
data/analysis/*.csv
```

具体输入如下：

C 要求 B 保证这些文件字段稳定。如果 C 发现某个图表需要额外指标，应反馈给 B 增加分析结果表，而不是自己在可视化端临时重写分析逻辑。

---

## 2\. C 的真实输出文件

C 的主要输出应写成这样：

```Plain Text
src/visualization/
├── app.py              # Streamlit 网页主入口，最终可交互看板
├── charts.py           # Plotly 图表函数
└── utils.py            # 可选：读取数据、筛选数据、格式化金额/日期
```

辅助输出：

```Plain Text
outputs/screenshots/
├── dashboard_home.png              # 看板首页截图
├── dashboard_filter_example.png    # 筛选交互效果截图
└── dashboard_detail_table.png      # 明细表截图
```

## 3\. 看板整体结构

网页标题建议：

```Plain Text
金融机构监管风险与舆情预警看板
```

页面布局建议分为 7 个区域：

---

## 4\. 交互逻辑设计

C 需要实现的交互不是“选择生成哪张 PNG”，而是：

```Plain Text
网页读取 CSV
→ 用户选择筛选条件
→ 页面实时筛选数据
→ 图表和表格同步更新
```

例如：

```Plain Text
用户选择：
时间范围 = 2024-01 到 2025-12
风险类型 = 合规风险
风险等级 = 高
地区 = 广东
数据来源 = regulation/news/comment

页面自动更新：
总览指标
风险趋势图
风险类型图
地区排行
主体 Top 榜
舆情趋势图
事件明细表
```

### 必须实现的筛选器

### 图表交互要求

Plotly 默认支持悬停、图例开关、缩放，所以使用 Plotly 比 matplotlib 更合适。

---

## 5\. 每个页面模块的具体要求（可参考，可灵活调整）

### 5\.1 顶部总览区

输入：

```Plain Text
summary_stats.csv
risk_event_table.csv
```

展示指标：

注意：如果用户使用筛选器，指标卡片应根据筛选后的数据重新计算；如果技术上来不及，可以先展示 B 的总览数据，但最好实现动态更新。

---

### 5\.2 时间趋势区

输入：

```Plain Text
risk_time_trend.csv
```

图表：

```Plain Text
折线图 / 双轴折线图
```

展示内容：

交互：

```Plain Text
可按时间范围缩放；
悬停显示月份、数量、金额；
图例可点击隐藏/显示。
```

---

### 5\.3 风险类型结构区

输入：

```Plain Text
risk_type_stats.csv
```

图表：

```Plain Text
柱状图 + 饼图二选一，最好柱状图为主
```

展示字段：

交互：

```Plain Text
悬停显示风险类型、事件数、处罚金额、平均风险分；
可按事件数或平均风险分排序。
```

---

### 5\.4 地区与主体区

输入：

```Plain Text
region_risk_stats.csv
entity_risk_rank.csv
```

图表 1：地区风险排行

```Plain Text
横向柱状图
```

字段：

```Plain Text
region,event_count,total_penalty_amount,high_risk_count,avg_risk_score
```

图表 2：高风险主体 Top 10

```Plain Text
条形图或表格
```

字段：

```Plain Text
entity_name,entity_type,region,event_count,total_penalty_amount,avg_risk_score,risk_level
```

交互：

```Plain Text
可以选择 Top N，例如 Top 10 / Top 20；
可以选择排序指标：事件数 / 处罚金额 / 平均风险分。
```

如果地图实现有难度，不强制做地图。横向柱状图更稳定，也容易解释。

---

### 5\.5 舆情联动区

输入：

```Plain Text
sentiment_trend.csv
risk_time_trend.csv
```

图表：

```Plain Text
双轴折线图
```

展示内容：

目标：展示“监管处罚事件”和“负面舆情变化”是否存在同向变化。

注意：这里不要声称舆情能准确预测风险，只能说“舆情可作为辅助观察信号”。

---

### 5\.6 风险关系热力图

输入：

```Plain Text
risk_relation_matrix.csv
```

图表：

```Plain Text
热力图
```

字段：

```Plain Text
entity_type,risk_type,event_count,avg_risk_score
```

展示逻辑：

```Plain Text
横轴 = 风险类型
纵轴 = 主体类型
颜色深浅 = 事件数量或平均风险分
```

交互：

```Plain Text
悬停显示主体类型、风险类型、事件数、平均风险分。
```

---

### 5\.7 风险事件明细区

输入：

```Plain Text
risk_event_table.csv
```

展示形式：

```Plain Text
可交互表格
```

显示字段：

```Plain Text
event_date
source_type
entity_name
entity_type
region
risk_type
risk_level
risk_score
sentiment
penalty_amount
summary
url
```

交互：

```Plain Text
支持关键词搜索；
支持按风险等级、风险类型、地区筛选；
支持点击/复制 URL 查看原文；
支持按 risk_score 排序。
```

这部分很重要，因为它能证明图表背后有真实数据，不只是聚合结果。

---

## 6\. C 的代码结构建议

`app.py` 负责页面布局和交互：

```Plain Text
import streamlit as st
from utils import load_all_data, apply_filters
from charts import (
    plot_time_trend,
    plot_risk_type_bar,
    plot_region_rank,
    plot_entity_rank,
    plot_sentiment_trend,
    plot_relation_heatmap,
)

st.set_page_config(page_title="金融风险预警看板", layout="wide")

# 1. 读取数据
# 2. 渲染筛选器
# 3. 根据筛选条件过滤数据
# 4. 渲染指标卡片
# 5. 渲染各类图表
# 6. 渲染事件明细表
```

`utils.py` 负责数据读取和筛选：

```Plain Text
def load_all_data(data_dir: str):
    """
    读取 risk_event_table.csv 和 analysis/*.csv
    返回多个 DataFrame。
    """

def apply_filters(df, filters):
    """
    根据时间、来源、风险类型、风险等级、地区、主体类型、关键词筛选 risk_event_table。
    """
```

`charts.py` 负责图表函数：

```Plain Text
def plot_time_trend(df):
    """绘制风险时间趋势图"""

def plot_risk_type_bar(df):
    """绘制风险类型分布图"""

def plot_region_rank(df):
    """绘制地区风险排行图"""

def plot_entity_rank(df):
    """绘制高风险主体排行图"""

def plot_sentiment_trend(df):
    """绘制舆情趋势图"""

def plot_relation_heatmap(df):
    """绘制主体类型-风险类型热力图"""
```

---

## 7\. C 的开发顺序

C 不需要等 B 完全完成再开始。建议这样推进：

模拟数据可以先按照 B 的字段自己造几行，只要字段名一致，后期替换真实数据即可。

---

## 8\. C 的交接与验收标准

C 最终交给全组：

```Plain Text
src/visualization/app.py
src/visualization/charts.py
src/visualization/utils.py
outputs/screenshots/*.png
README 中的看板运行说明
```

C 的完成标准：

```Plain Text
1. 看板可以通过 streamlit run src/visualization/app.py 正常启动；
2. 页面能成功读取 B 输出的 risk_event_table.csv 和 analysis/*.csv；
3. 至少有 5 类图表，理想为 7 个模块；
4. 至少有 5 个筛选器：时间、风险类型、风险等级、地区、主体类型；
5. 图表支持悬停查看详情；
6. 筛选条件变化后，主要图表和明细表能同步变化；
7. 有事件明细表，能看到具体风险事件和原文 URL；
8. 截图仅作为报告/PPT 材料，不替代网页本身。
```

## 三个角色共同注意事项

最终交付包：

```Plain Text
研究实践报告.docx
课堂汇报PPT.pptx
代码压缩包.zip
数据文件压缩包.zip
skills目录
AI工具使用说明与截图
运行日志
人工审核表
可视化截图
```

这个分工的核心逻辑是：

```Plain Text
A 保证数据进得来、格式初步干净；
B 保证文本能被大模型理解并转成风险指标；
C 保证分析结果能被清楚展示和讲明白。
```



